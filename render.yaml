services:
  - type: web
    name: v1-video-downloader-api
    env: docker
    plan: free
    autoDeploy: true

    dockerfilePath: ./Dockerfile

    envVars:
      - key: PORT
        value: 8000

    healthCheckPath: /endpoints